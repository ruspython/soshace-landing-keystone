<main id="app" class="main-section">
	{{#with data}}
		{{>home}}
		{{>about}}
		{{>resume}}
		{{>portfolio}}
		{{>team}}
		{{>funfacts}}
		{{>testimonial}}
		{{>blog}}
		{{>contact}}
	{{/with}}
	{{>footer}}
</main>
