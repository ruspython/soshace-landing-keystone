<main id="app" class="main-section">
	{{>header}}
	{{#with data}}
		{{>home}}
		{{>about}}
		{{>resume}}
		{{>portfolio}}
		{{>team}}
		{{>funfacts}}
		{{>testimonial}}
		{{>contact}}
	{{/with}}
	{{>footer}}
</main>
