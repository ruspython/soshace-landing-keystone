<main id="app" class="main-section">
	{{>header}}
	{{#with data}}
		{{>intro}}
		{{>features}}
		{{!-- {{>resume}} --}}
		{{!-- {{>portfolio}} --}}
		{{!-- {{>team}} --}}
		{{!-- {{>funfacts}} --}}
		{{!-- {{>testimonial}} --}}
		{{!-- {{>contact}} --}}
	{{/with}}
	{{!-- {{>footer}} --}}
</main>
